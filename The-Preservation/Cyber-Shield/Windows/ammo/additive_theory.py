"""追加论计算原子（预留）。

反向检索攻击者所在公开群，多群叠加举报。
仅使用公开可检索信息，不社工非授权数据。
"""


def find_public_groups(target_account: str) -> list:
    """（预留）检索目标账号参与的公开群。"""
    raise NotImplementedError("追加论待实现")


def build_multi_group_ammo(groups: list, base_ammo: dict) -> list:
    """（预留）基于多个群生成叠加弹药。"""
    raise NotImplementedError("追加论待实现")
